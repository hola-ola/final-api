const LENGTH_OF_STAY_ENUM = [
  "Less than a week",
  "Weekend",
  "Up to two weeks",
  "Up to a month",
  "Long stay",
];

const LISTING_TYPE_ENUM = ["House", "Apartment", "Room"];

module.exports = LENGTH_OF_STAY_ENUM;
module.exports = LISTING_TYPE_ENUM;
